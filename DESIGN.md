# Java Web 安全情报社区平台（毕业设计）方案草案

## 1. 项目目标
- 构建一个面向安全情报分享与协作的社区平台，支持情报发布、讨论、关联、检索与知识沉淀。
- 覆盖“情报生命周期”：采集 → 处理 → 分析 → 共享 → 反馈。
- 具备基础的合规与安全能力（审计、权限、内容审核）。

## 2. 角色与权限
- **游客**：浏览公开情报、搜索、查看公告。
- **注册用户**：发布情报、评论、收藏、关注标签/作者。
- **版主**：内容审核、违规处理、标签维护。
- **管理员**：用户管理、权限分配、系统配置、审计日志。

## 3. 核心功能模块
### 3.1 情报发布与管理
- 发布情报（标题、摘要、正文、情报类型、可信度、来源、标签、IOC 等）。
- 支持 Markdown 编辑与预览。
- 情报状态流转（草稿/待审/已发布/驳回/下架）。

### 3.2 情报检索与推荐
- 关键词搜索、标签筛选、时间范围筛选、可信度筛选。
- 热门/最新/高质量情报推荐。
- 基于用户关注标签的个性化推荐。

### 3.3 讨论与协作
- 评论与回复（楼中楼）。
- 点赞/收藏/分享。
- 关注作者与标签。

### 3.4 情报结构化与关联
- IOC 结构化字段（IP、域名、Hash、URL 等）。
- 情报关联（关联历史事件、漏洞编号、攻击组织）。
- 情报可信度评分模型（用户投票 + 版主评估）。

### 3.5 运营与治理
- 举报与审核流程。
- 黑名单与敏感词过滤。
- 审计日志与操作记录。

## 4. 技术栈建议
- **后端**：Spring Boot + Spring MVC + Spring Security + MyBatis-Plus。
- **数据库**：MySQL 8.x；缓存 Redis。
- **搜索**：Elasticsearch（可选）或 MySQL LIKE/全文索引（毕业设计简化）。
- **前端**：Vue 3 + Vite + Element Plus。
- **文件存储**：本地/MinIO（可选）。
- **部署**：Docker Compose（可选）。

## 5. 数据库设计（核心表）
- `users`：用户基本信息、角色、状态。
- `roles` / `user_roles`：角色与权限。
- `intel_posts`：情报主体（标题、内容、状态、可信度）。
- `intel_iocs`：IOC 表（与情报关联）。
- `tags` / `post_tags`：标签体系。
- `comments`：评论与回复。
- `favorites` / `likes`：收藏与点赞。
- `reports`：举报记录。
- `audit_logs`：审计日志。

## 6. 安全与合规设计
- **认证与授权**：JWT + RBAC。
- **输入校验**：XSS/SQL 注入防护、敏感词过滤。
- **内容审计**：发布前审核与违规处置。
- **操作审计**：关键操作写入审计日志。
- **接口限流**：防刷与保护关键接口。

## 7. 里程碑与验收
1. **需求分析 & 原型**（第 1-2 周）
2. **基础框架搭建**（第 3 周）
3. **核心模块开发**（第 4-7 周）
4. **搜索/推荐与优化**（第 8-9 周）
5. **测试与答辩准备**（第 10 周）

## 8. 论文写作建议
- 需求分析、系统架构设计、核心模块设计、数据结构设计。
- 安全策略与权限模型设计。
- 系统测试与性能评估。
- 结论与展望。
